# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ClouDoc_iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ClouDoc_iOS
  
  # http library
  pod 'Alamofire', '~> 5.5'

  # RX
  pod 'RxSwift', '6.5.0'
  pod 'RxCocoa', '6.5.0'
  pod 'RxDataSources', '~> 5.0'
  # UI
  pod 'SnapKit', '~> 5.0.0'
  pod 'Then', '~> 2.7.0'

  # 네트워크 확인 framework
  # pod 'ReachabilitySwift', '~> 5.0'
  
  #multi image picker https://github.com/guillermomuntaner/GMImagePicker
  #pod 'GMImagePicker', '~> 0.0.2'

  # orderedDic
  # pod 'OrderedDictionary', '~> 1.4'
  
  # FireBase
  # pod 'Firebase/Analytics', '~> 7.2'
  # pod 'Firebase/Messaging', '~> 7.2'

  target 'ClouDoc_iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ClouDoc_iOSUITests' do
    # Pods for testing
  end

end
